setono_sylius_quickpay:
    gateway_configuration:
        api_key:
            invalid: 'Quickpay rejected this API key. Check that it is the API key of the API user in your Quickpay manager (Settings → Users).'
